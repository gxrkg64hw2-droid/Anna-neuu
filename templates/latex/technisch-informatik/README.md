# Profil: technisch-informatik

Fuer Bachelorarbeiten in Wirtschaftsinformatik, Informatik, Software Engineering, AI/Data Science.

**Kapitelstruktur:** Einleitung -> Grundlagen -> Konzept -> Implementierung -> Evaluation -> Fazit.

**Profil-spezifische Pakete:** `listings` (Code-Listings), `algorithm2e` (Pseudocode), erweiterte TikZ-Bibliotheken.

**Compile:**

```bash
cd thesen/<name>/tex && latexmk -lualatex main.tex
```

**Empfohlene Skills (auto-trigger via Bridge-Skill):**

- `scientific-writing` fuer Einleitung, Grundlagen, Fazit
- `literature-review` fuer Stand-der-Forschung
- `scientific-critical-thinking` fuer Diskussion

Beispiel-Architekturdiagramm in `kapitel/03-konzept.tex` als Vorlage.
